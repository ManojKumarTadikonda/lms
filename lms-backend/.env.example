# ---------- Postgres (local Docker DB) ----------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_db_password_here
POSTGRES_DB=library

# ---------- Database URL ----------
# Option A: Local Postgres in Docker (db service)
DATABASE_URL=postgresql://postgres:your_db_password_here@db:5432/library?schema=public

# Option B: Supabase (uncomment and fill, then comment Option A)
# DATABASE_URL=postgresql://postgres:YOUR_SUPABASE_PASSWORD@YOUR_SUPABASE_HOST:5432/postgres?schema=public

# ---------- Auth ----------
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=86400
